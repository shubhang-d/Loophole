<?xml version="1.0" encoding="utf-8"?>
<!--
  Static placeholder shown in the widget picker and as the initial layout before
  the live Glance widget renders. Mirrors the Glance chip: status dot + ON/OFF
  label on the left, circular gear button on the right.

  Only RemoteViews-safe views are used (LinearLayout, FrameLayout, ImageView,
  TextView) — a bare View or Space is rejected by RemoteViews inflation and makes
  the launcher fail with "Couldn't add widget".
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="48dp"
    android:id="@+id/root"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/widget_preview_bg"
    android:paddingLeft="18dp"
    android:paddingRight="18dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <ImageView
        android:layout_width="12dp"
        android:layout_height="12dp"
        android:background="@drawable/widget_preview_dot"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:text="OFF"
        android:id="@+id/onOrOff"
        android:textColor="#E3E3E3"
        android:textSize="26sp"
        android:textStyle="bold" />

    <FrameLayout
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="12dp"
        android:background="@drawable/widget_preview_gear_bg">

        <ImageView
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_settings_gear"
            android:tint="#2A2A2A"
            android:contentDescription="@null" />
    </FrameLayout>
</LinearLayout>